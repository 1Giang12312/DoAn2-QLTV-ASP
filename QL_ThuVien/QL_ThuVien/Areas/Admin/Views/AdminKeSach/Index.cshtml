@model IEnumerable<QL_ThuVien.Models.TblKeSach>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StTenKeSach)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StTenKeSach)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StMaKeSach">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StMaKeSach">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StMaKeSach">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
