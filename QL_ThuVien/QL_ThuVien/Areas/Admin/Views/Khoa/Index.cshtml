@model IEnumerable<QL_ThuVien.Models.TblKhoa>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h1>Quản lý khoa</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StTenKhoa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StTenKhoa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StMaKhoa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StMaKhoa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StMaKhoa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
