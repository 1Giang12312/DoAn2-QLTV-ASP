@model IEnumerable<QL_ThuVien.Models.TblLoaiSach>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StTenSach)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StTenSach)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StMaLoaiSach">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StMaLoaiSach">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StMaLoaiSach">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
