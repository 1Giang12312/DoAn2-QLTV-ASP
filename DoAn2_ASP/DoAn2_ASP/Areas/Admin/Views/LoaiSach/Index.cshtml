@model IEnumerable<DoAn2_ASP.Models.TblLoaiSach>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h1>Loại sách</h1>

<p>
    <a asp-action="Create">Thêm mới</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StTenSach)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StTenSach)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StMaLoaiSach">Chỉnh sửa</a> |
                <a asp-action="Details" asp-route-id="@item.StMaLoaiSach">Chi tiết</a> |
                <a asp-action="Delete" asp-route-id="@item.StMaLoaiSach">Xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
