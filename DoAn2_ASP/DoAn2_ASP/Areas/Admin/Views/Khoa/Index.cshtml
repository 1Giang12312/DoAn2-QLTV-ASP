@model IEnumerable<DoAn2_ASP.Models.TblKhoa>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h1>Khoa</h1>

<p>
    <a asp-action="Create">Thêm mới</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StTenKhoa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StTenKhoa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StMaKhoa">Chỉnh sửa</a> |
                <a asp-action="Details" asp-route-id="@item.StMaKhoa">Chi tiết</a> |
                <a asp-action="Delete" asp-route-id="@item.StMaKhoa">Xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
